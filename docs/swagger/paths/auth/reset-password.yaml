tags:
  - Auth
summary: Reset password
description: Resets password using token
operationId: resetPasswordUser
security:
  - bearerAuth: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/user.yaml#/ResetPassword'
responses:
  '200':
    description: Password reset successfully
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/common.yaml#/DefaultResponse'
  '401':
    $ref: '../../components/responses/401.yaml'
